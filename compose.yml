name: rolerover

services:
  app:
    container_name: rolerover
    build:
      context: .
    image: rolerover:latest
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
    volumes:
      - ./data:/app/data
